import { View, Text } from 'react-native';

export default function DoctorsScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Doctors Screen (Sẽ bổ sung giao diện sau)</Text>
    </View>
  );
}
